# 無音の補集合には必ず「端の断片」フィルタが要る

無音区間の補集合として「残す区間」を計算すると、収録の端や無音同士の境目に
0.1〜0.3秒のゴミ区間が必ず残る(無音検出の終端と動画の実尺のズレ+パディングが
原因)。最初の実走でいきなり 0.17 秒の断片が出た。

対策: 「残す区間が minKeepSec(0.5秒)未満なら捨てる」フィルタを補集合計算の
最後に入れる。無音カット系ツールを作るときは最初から入れておくべき定番処理。
